import React from 'react'

const TabsCode = ({}) => {
    return (
        <>
            <div role="tablist" className="tabs tabs-boxed">
                <a role="tab" className="tab">
                    <button onClick={() => filterSize(x)}>{x}</button>

                </a>
            </div>
        </>
    )
}

export default TabsCode